"use strict";
const logDetails = (
// uid: string | number,
uid, item) => {
    console.log(`${item} has a uid of ${uid}`);
};
const greet = (
// user: {
//   name: string,
//   uid: string | number
// }
user) => {
    console.log(`${user.name} says hello`);
};
const greet2 = (
// user: {
//   name: string,
//   uid: string | number
// }
user) => {
    console.log(`${user.name} says hello`);
};
